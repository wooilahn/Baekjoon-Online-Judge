#include <stdio.h>

using namespace std;

int main(){
  int N;
  scanf("%d", &N);

  if(N == 0)
    printf("YONSEI\n");
  else if(N == 1)
    printf("Leading the Way to the Future\n");

  return 0;
}
